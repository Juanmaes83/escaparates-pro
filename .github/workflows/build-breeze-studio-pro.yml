name: Build Breeze Studio PRO

on:
  push:
    branches:
      - feature/breeze-studio-pro
    paths:
      - .github/workflows/build-breeze-studio-pro.yml
      - scripts/build_breeze_studio_pro.py
      - scripts/build_breeze_studio_pro_v2.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Escaparates Pro
        uses: actions/checkout@v4
        with:
          ref: feature/breeze-studio-pro
          fetch-depth: 0

      - name: Checkout Breeze source
        uses: actions/checkout@v4
        with:
          repository: Juanmaes83/breeze
          ref: 0ab82342f9169f20e32b0e90babcc4707e694906
          path: breeze-source

      - name: Add derivative authoring layer V1
        run: python scripts/build_breeze_studio_pro.py

      - name: Add derivative authoring layer V2
        run: python scripts/build_breeze_studio_pro_v2.py

      - name: Build Breeze Studio PRO
        working-directory: breeze-source
        run: |
          npm ci
          npm run build

      - name: Install built module
        run: |
          set -euo pipefail
          DEST="labs/website-modules-source/breeze-studio-pro"
          rm -rf "$DEST"
          mkdir -p "$DEST"
          cp -R breeze-source/dist/. "$DEST/"
          cp breeze-source/LICENSE "$DEST/LICENSE"
          cp breeze-source/CREDITS.md "$DEST/CREDITS.md"
          cat > "$DEST/README.md" <<'EOF'
          # Breeze Studio PRO

          Independent Escaparates Pro module built additively from the Breeze engine.

          Source engine:
          - Juanmaes83/breeze
          - commit 0ab82342f9169f20e32b0e90babcc4707e694906
          - upstream lineage: holtsetio/breeze

          Derivative additions only:
          - custom authoring panel;
          - robust image/video mapped onto the simulated cloth;
          - cloth media Scale/X/Y;
          - custom background image/video while preserving HDRI lighting;
          - replaceable GLB/GLTF/OBJ 3D object with auto-normalization;
          - uploaded 3D geometry becomes the real BVH cloth collider after scene rebuild;
          - scene, camera, simulation, stiffness and friction controls;
          - PNG, WebM and clean preview.

          Banderolas Dinamicas remains untouched and is not a runtime dependency.
          Its proven non-blocking video-loading pattern informed the V2 implementation.
          EOF

      - name: Register Website Module
        run: |
          cat > js/website-modules-breeze-studio-pro.js <<'EOF'
          (function() {
              'use strict';
              window.EP = window.EP || {};
              if (!EP.WebsiteModules || !EP.WebsiteModules.register) return;
              var PATH = 'labs/website-modules-source/breeze-studio-pro/index.html';
              EP.WebsiteModules.register({
                  id: 'breeze-studio-pro',
                  name: 'Breeze Studio PRO',
                  icon: 'BR',
                  family: 'Interactive Media / Dynamic Fabric',
                  description: 'Escultura y tela WebGPU con fisica Verlet, media en tela, fondos personalizados y objeto 3D reemplazable.',
                  sourceFile: 'Juanmaes83/breeze · 0ab82342f9169f20e32b0e90babcc4707e694906 · additive derivative V2',
                  mediaMap: 'Imagen/video en tela + fondo imagen/video + GLB/GLTF/OBJ + Scale/X/Y + Scene/Camera/Physics + PNG/WebM/Preview Clean.',
                  standalonePath: PATH,
                  standaloneEditorNote: 'Modulo nuevo e independiente. Breeze preservado como motor; Banderolas permanece intacto y no es dependencia runtime.',
                  fields: [],
                  build: function() {
                      return '<!doctype html><html><body style="margin:0;background:#000"><iframe src="' + PATH + '" style="position:fixed;inset:0;width:100%;height:100%;border:0" allow="autoplay;fullscreen" allowfullscreen></iframe></body></html>';
                  }
              });
          })();
          EOF
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          tag = '<script src="js/website-modules-breeze-studio-pro.js"></script>'
          if tag not in s:
              anchor = '<script src="js/website-modules-banderolas-output-v2.js"></script>'
              if anchor not in s:
                  raise SystemExit('Website Modules anchor not found')
              s = s.replace(anchor, anchor + '\n' + tag, 1)
              p.write_text(s, encoding='utf-8')
          PY

      - name: QA
        run: |
          set -euo pipefail
          test -f labs/website-modules-source/breeze-studio-pro/index.html
          test -f labs/website-modules-source/breeze-studio-pro/LICENSE
          test -f labs/website-modules-source/breeze-studio-pro/CREDITS.md
          grep -R -q 'Breeze Studio PRO' labs/website-modules-source/breeze-studio-pro
          grep -q 'website-modules-breeze-studio-pro.js' index.html
          grep -R -q 'Background' labs/website-modules-source/breeze-studio-pro/assets
          grep -R -q 'Restore Venus' labs/website-modules-source/breeze-studio-pro/assets
          node --check js/website-modules-breeze-studio-pro.js
          git diff --check

      - name: Commit candidate
        run: |
          set -euo pipefail
          rm -rf breeze-source
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html js/website-modules-breeze-studio-pro.js labs/website-modules-source/breeze-studio-pro
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "feat(web-modules): upgrade Breeze Studio PRO V2"
          git push origin HEAD:feature/breeze-studio-pro
