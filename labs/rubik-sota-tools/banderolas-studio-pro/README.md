# Banderolas Studio PRO — historical Type B entry note

Status: **SUPERSEDED AS CANONICAL PLACEMENT / preserved for traceability**.

This path was created during the first Type B integration attempt under RUBIK SOTA. Do not delete it while the recovery/integration programme is active, but do not use it as the canonical architecture record.

Canonical documentation now lives at:

`labs/immersive-worlds/shared-capabilities/flexible-media/banderolas/README.md`

Canonical capability classification:

`Escaparates Pro → Immersive Worlds → Shared Capabilities → Flexible Media / Dynamic Fabric`

Current integration rule:

`PRESERVE LOCAL SOURCE → ADAPTER → PANEL → QA → APPROVAL`

The earlier runtime-fetch / in-memory source-rewrite approach described by the first revision of this file was rejected after runtime failures. The active implementation preserves Output V2 byte-for-byte inside Escaparates Pro and delegates through a same-origin DOM adapter.

The existing RUBIK SOTA catalog card may remain temporarily as a visual-review entry point until Juanma approves the final UI placement. It must not be interpreted as the canonical product-family classification.

No merge into `master` is authorized without explicit Juanma approval.